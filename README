This is the Ginga HoNeS library.

The autogen.sh script does all the magic calls to automake/autoconf and
friends that are needed to configure the compiling environment.

DEPENDENCIES
============
-> libtool  >= 1.3.4
-> autoconf >= 2.13
-> automake >= 1.4
-> directfb >= 1.0 (www.directfb.org)
-> gingacc-io (from gingacc-cpp) >= 0.9.27
   (http://svn.softwarepublico.gov.br/trac/ginga/browser/gingacc-cpp)
-> apache log4cxx >= 0.10 (http://logging.apache.org/log4cxx)
-> apache Xerces-C++ >= 3.0.1 (http://xerces.apache.org/xerces-c)

RECOMMENDED DEPENDENCIES
========================
These dependencies are optional. They provide software decoders for some types
of media, such as: Images (png, jpg...), Videos (MPEG, H264...), Audios...
-> libpng
-> libjpg
-> xine-lib >= 1.1.7 (http://www.xine-project.org/)
-> essential >= 20061022 (http://www.mplayerhq.hu/MPlayer/releases/codecs/)
-> directfb-extra >= 1.0 (www.directfb.org)
-> apache portable runtime >= 1.3.3 (http://apr.apache.org/)
-> apache portable runtime util >= 1.3.4 (http://apr.apache.org/)

INSTALLATION
============
As mentioned above, autogen.sh does all the `configure' magic.
So the installation steps are:
   $ ./autogen.sh
   $ make
   $ make install
If you need to customize any path or regular `configure' value, please
refer to the INSTALL file.
